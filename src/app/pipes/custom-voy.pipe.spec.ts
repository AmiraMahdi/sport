import { CustomVoyPipe } from './custom-voy.pipe';

describe('CustomVoyPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomVoyPipe();
    expect(pipe).toBeTruthy();
  });
});
